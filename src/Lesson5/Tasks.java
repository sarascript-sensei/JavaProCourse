package Lesson5;

public class Tasks {
    //1 задача
    /*Создайте ArrayList и заполните его целочисленными значениями
    Добавьте элемент "InseredElement" в индекс 3
    Вывод:
ArrayList contains...
1
2
3
INSERTED ELEMENT
4
5
*/

    //2 задача
    /*Создайте ArrayList и заполните его целочисленными значениями
    Найдите определенный элемент в списке, если же его нет выполните условие: если элемента нет вывод будет:
     Элемента ... нет в списке(Помним, что метод contains
    в случае отсуствия элемента возвращает -1)*/

    //3 задача
    /*Создайте ArrayList и заполните его целочисленными значениями
    Удалите элемент с индексом 1 из списка*/

    //4 задача
    /*
    1.Создайте класс Test
    2. В нем создайте ArrayList со строковыми значениями letters
    3.Выполните все пройденные операции за сегодня так, чтоб вывод всей программы был примерно таким:
    [A, B, C, D]
    [A, B, C, D, E, H]
    [A, B, C, D, E, F, G, H]
    Letters list contains E ? true
    Letters list contains Z ? false
    Letter at 5th place: E
    tempList is empty ? true
    First index of D = 3
    Last index of D = 3
    [A, B, C, D, E, F, G, H]
    After removing 'D' letters contains [A, B, C, E, F, G, H]
    H removed? true. Letters contains [A, B, C, E, F, G]
    list contains [F, G]
    [A, B, C, E]
    letters elements after retainAll operation: [A, B, C]
    letters ArrayList size = 3
    [A, B, D]
     */
}
